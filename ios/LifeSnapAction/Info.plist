<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Required app bundle metadata -->
    <key>CFBundleDevelopmentRegion</key>
    <string>ja</string>
    <key>CFBundleDisplayName</key>
    <string>よていスナップ</string>
    <key>CFBundleExecutable</key>
    <string>$(EXECUTABLE_NAME)</string>
    <key>CFBundleIdentifier</key>
    <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
    <key>CFBundleInfoDictionaryVersion</key>
    <string>6.0</string>
    <key>CFBundleName</key>
    <string>$(PRODUCT_NAME)</string>
    <key>CFBundlePackageType</key>
    <string>APPL</string>
    <key>CFBundleShortVersionString</key>
    <string>$(MARKETING_VERSION)</string>
    <key>CFBundleVersion</key>
    <string>$(CURRENT_PROJECT_VERSION)</string>
    <key>UILaunchStoryboardName</key>
    <string>LaunchScreen</string>

    <!-- Full-screen portrait-only app configuration -->
    <key>UIRequiresFullScreen</key>
    <true/>
    <key>UISupportedInterfaceOrientations</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
    </array>
    <key>UISupportedInterfaceOrientations~ipad</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
    </array>

    <!-- Camera usage -->
    <key>NSCameraUsageDescription</key>
    <string>撮影した書類画像から予定候補を抽出し、確認後にカレンダーへ追加するために使用します。</string>

    <!-- Photo library usage -->
    <key>NSPhotoLibraryUsageDescription</key>
    <string>選択した書類画像から予定候補を抽出し、確認後にカレンダーへ追加するために使用します。</string>

    <!-- Calendar write-only (iOS 17+) -->
    <key>NSCalendarsWriteOnlyAccessUsageDescription</key>
    <string>ユーザーが確認した予定をシステムカレンダーに追加するために使用します。</string>

    <!-- Calendar full access fallback (iOS 16) -->
    <key>NSCalendarsUsageDescription</key>
    <string>ユーザーが確認した予定をシステムカレンダーに追加するために使用します。</string>
</dict>
</plist>
